import {useState} from 'react';import {Link,useLocation,useNavigate} from 'react-router-dom';import {ArrowLeft,Eye,EyeOff,LockKeyhole,Mail,ShieldCheck,Sparkles,CheckCircle2} from 'lucide-react';import {api,authStore} from '../api';
export default function Login(){const [email,setEmail]=useState('demo@subsight.cn');const [password,setPassword]=useState('Demo123456');const [show,setShow]=useState(false);const [remember,setRemember]=useState(true);const [loading,setLoading]=useState(false);const [error,setError]=useState('');const nav=useNavigate();const location=useLocation();const submit=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);setError('');try{const result=await api.login(email,password);authStore.set(result.token,result.user);if(!remember)sessionStorage.setItem('subsight-session-only','true');nav((location.state as {from?:string}|null)?.from||'/dashboard',{replace:true})}catch(err){setError(err instanceof Error?err.message:'登录失败，请重试')}finally{setLoading(false)}};return <div className="login-page"><section className="login-brand-panel"><Link to="/" className="brand login-brand"><span className="brand-mark">S</span><span>苏策通<small>Subsight</small></span></Link><div className="login-pitch"><span className="eyebrow"><Sparkles/> 苏州企业政策智能申报助手</span><h1>让每一家企业，<br/>都不错过合适的政策。</h1><p>从政策匹配、资质核验到材料预审，用一套清晰可解释的流程完成申报准备。</p><ul><li><CheckCircle2/>政策来源可追溯</li><li><CheckCircle2/>匹配依据可解释</li><li><CheckCircle2/>企业数据本地可控</li></ul></div><div className="login-security"><ShieldCheck/><span><strong>安全与边界</strong>平台仅提供企业端辅助服务，不替代政府审核与申报提交。</span></div></section><section className="login-form-panel"><Link to="/" className="back-link"><ArrowLeft/>返回产品首页</Link><form className="login-card" onSubmit={submit}><div><span className="login-kicker">WELCOME BACK</span><h2>登录企业工作台</h2><p>使用企业账号继续管理政策匹配与申报项目</p></div><label className="login-field"><span>登录邮箱</span><div><Mail/><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email"/></div></label><label className="login-field"><span>登录密码</span><div><LockKeyhole/><input type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/><button type="button" onClick={()=>setShow(!show)} aria-label="显示密码">{show?<EyeOff/>:<Eye/>}</button></div></label><div className="login-options"><label><input type="checkbox" checked={remember} onChange={e=>setRemember(e.target.checked)}/>记住登录状态</label><button type="button" onClick={()=>setError('Demo 账号无需找回密码，请使用页面下方提供的账号。')}>忘记密码？</button></div>{error&&<div className="login-error">{error}</div>}<button className="btn primary login-submit" disabled={loading}>{loading?<><i className="login-spinner"/>正在验证账号…</>:'登录工作台'}</button><div className="demo-account"><strong>比赛演示账号</strong><span>邮箱：demo@subsight.cn</span><span>密码：Demo123456</span><button type="button" onClick={()=>{setEmail('demo@subsight.cn');setPassword('Demo123456')}}>一键填入</button></div><p className="data-note"><ShieldCheck/>账号与企业数据保存在本地 SQLite 数据库中，未连接真实政府系统。</p></form></section></div>}
