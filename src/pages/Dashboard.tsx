import {useState} from 'react';import {AreaChart,Area,XAxis,YAxis,CartesianGrid,Tooltip,ResponsiveContainer} from 'recharts';import {ArrowUpRight,FileText,Clock,CheckSquare,Sparkles} from 'lucide-react';import {enterprise,policies,trend} from '../data';import {PolicyCard} from '../components/UI';
export default function Dashboard(){const [toast,setToast]=useState('');const notify=(s:string)=>{setToast(s);setTimeout(()=>setToast(''),2200)};return <div><div className="page-hero compact"><div><span className="eyebrow">企业工作台</span><h1>早上好，{enterprise.name}</h1><p>系统已根据您的企业档案更新政策匹配结果。</p></div><button className="btn primary" onClick={()=>notify('已完成政策库扫描，新增 2 条可匹配政策')}><Sparkles size={18}/> 更新匹配结果</button></div><div className="stats-grid">{[['可申报政策','8 项','+2 本周',ArrowUpRight],['高度匹配政策','3 项','匹配度 ≥ 75%',CheckSquare],['待补充材料','5 项','2 项较紧急',FileText],['即将截止政策','2 项','最近 16 天',Clock]].map(([a,b,c,I])=><div className="stat card" key={String(a)}><span><I/></span><small>{String(a)}</small><strong>{String(b)}</strong><em>{String(c)}</em></div>)}</div><div className="dash-grid"><section className="card chart-card"><div className="card-title"><div><h2>政策匹配趋势</h2><p>近 6 个月新增政策与企业可匹配数量</p></div><span className="badge green">持续更新</span></div><ResponsiveContainer width="100%" height={260}><AreaChart data={trend}><defs><linearGradient id="g1" x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="#1E63FF" stopOpacity={.25}/><stop offset="95%" stopColor="#1E63FF" stopOpacity={0}/></linearGradient></defs><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="m" axisLine={false}/><YAxis axisLine={false}/><Tooltip/><Area type="monotone" dataKey="all" stroke="#A7B4C8" fill="transparent" name="新增政策"/><Area type="monotone" dataKey="match" stroke="#1E63FF" fill="url(#g1)" strokeWidth={3} name="可匹配"/></AreaChart></ResponsiveContainer></section><section className="card todo-card"><div className="card-title"><div><h2>今日待办</h2><p>建议优先处理高风险事项</p></div><b>3 / 5</b></div>{['补充 2025 年研发费用数据','上传最新纳税证明','完成技改项目材料预审','确认高企申报联系人','查看即将截止政策'].map((x,i)=><label key={x}><input type="checkbox" defaultChecked={i>2} onChange={()=>notify('待办状态已保存')}/><span>{x}</span><em>{i<2?'重要':'普通'}</em></label>)}</section></div><div className="section-head"><div><h2>重点推荐政策</h2><p>基于当前企业档案和公开政策条件计算</p></div><a href="/match">查看全部 →</a></div>{policies.slice(0,3).map(p=><PolicyCard key={p.id} policy={p} onToast={notify}/>)}{toast&&<div className="toast">✓ {toast}</div>}</div>}
